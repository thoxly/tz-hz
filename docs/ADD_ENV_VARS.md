# Как добавить переменные в .env файл

## 📝 Формат переменных

В `.env` файле переменные должны быть в формате:

```env
КЛЮЧ=значение
```

**Важно:**
- Без пробелов вокруг `=`
- Без кавычек (если только значение не содержит пробелы)
- Каждая переменная на новой строке

## ✅ Правильный формат

```env
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-ваш_ключ_здесь
DEEPSEEK_BASE_URL=https://api.deepseek.com
```

## ❌ Неправильный формат

```env
# С пробелами (НЕПРАВИЛЬНО)
LLM_PROVIDER = deepseek

# С кавычками (НЕПРАВИЛЬНО, если не нужны)
DEEPSEEK_API_KEY="sk-ваш_ключ"

# Без значения (НЕПРАВИЛЬНО)
DEEPSEEK_API_KEY=
```

## 🔧 Быстрое добавление

Откройте `.env` файл и добавьте в конец:

```env
# LLM Settings
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=ваш_ключ_здесь
DEEPSEEK_BASE_URL=https://api.deepseek.com
```

**Замените `ваш_ключ_здесь` на ваш реальный ключ DeepSeek!**

## ✅ Проверка

После добавления проверьте:

```bash
python scripts/check_env_vars.py
```

Скрипт покажет все найденные LLM переменные.

## 🚀 После добавления

1. Сохраните `.env` файл
2. Перезапустите приложение (если оно запущено)
3. Проверьте работу: `python scripts/test_llm.py`

