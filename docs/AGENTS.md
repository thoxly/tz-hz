# Агенты обработки требований

Система автоматического создания ТЗ состоит из трех агентов, которые последовательно обрабатывают требования клиента.

## 🧩 Архитектура агентов

```
Транскрибация встречи
        ↓
[1] Process Extractor → AS-IS процесс
        ↓
[2] Architect Agent → Архитектура ELMA365
        ↓
[3] Scope Agent → Краткое ТЗ для согласования
```

## 1. Process Extractor - Аналитик

**Что делает:** Превращает хаос транскрибации в структурированный процесс AS-IS.

**Задачи:**
- Выделяет роли (кто участвует)
- Выделяет шаги процесса
- Ставит шаги в правильный порядок
- Фиксирует бизнес-правила
- Отмечает проблемы, боли, дубли
- Указывает "неизвестно", если данные отсутствуют

**API:**
```bash
POST /api/agents/process-extract
```

**Пример запроса:**
```json
{
  "transcript": "У нас есть процесс согласования договоров. Сначала менеджер создает заявку, потом отправляет директору на согласование. Директор проверяет и либо одобряет, либо отклоняет. Проблема в том, что это все делается вручную через почту..."
}
```

**Пример ответа:**
```json
{
  "as_is": {
    "process_name": "Согласование договоров",
    "roles": ["Менеджер", "Директор"],
    "steps": [
      {
        "step_number": 1,
        "name": "Менеджер создает заявку",
        "actor": "Менеджер",
        "problems": ["Делается вручную"]
      },
      {
        "step_number": 2,
        "name": "Отправка директору на согласование",
        "actor": "Менеджер"
      },
      {
        "step_number": 3,
        "name": "Директор проверяет и одобряет/отклоняет",
        "actor": "Директор"
      }
    ],
    "problems": ["Все делается вручную через почту"],
    "confidence": 0.85
  }
}
```

## 2. Architect Agent - Инженер ELMA365

**Что делает:** Проектирует архитектуру решения на основе AS-IS процесса.

**Задачи:**
- Анализирует сложность задачи
- Выбирает минимально достаточный набор инструментов ELMA365
- Формирует понятную и обоснованную архитектуру
- Объясняет почему выбран именно такой подход

**API:**
```bash
POST /api/agents/architect
```

**Пример запроса:**
```json
{
  "as_is": {
    "process_name": "Согласование договоров",
    "steps": [...],
    "roles": ["Менеджер", "Директор"]
  }
}
```

**Пример ответа:**
```json
{
  "architecture": {
    "components": [
      {
        "component_type": "process",
        "name": "Согласование договоров",
        "purpose": "Автоматизация процесса"
      },
      {
        "component_type": "app",
        "name": "Договоры",
        "purpose": "Хранение и управление данными"
      }
    ],
    "roles": [
      {
        "role_name": "Менеджер",
        "permissions": ["read", "create"],
        "components_access": ["Договоры", "Согласование договоров"]
      }
    ],
    "complexity": "medium",
    "confidence": 0.75
  }
}
```

## 3. Scope Agent - Подрядчик

**Что делает:** Генерирует краткое ТЗ для согласования с заказчиком (1-2 страницы).

**Задачи:**
- Кратко описывает цель проекта
- Описывает текущее состояние (AS-IS)
- Перечисляет сущности для разработки
- Формализует функциональные требования
- Указывает роли и права

**НЕ включает:**
- Интерфейсы
- Поля форм
- Низкоуровневую логику

**API:**
```bash
POST /api/agents/scope
```

**Пример запроса:**
```json
{
  "as_is": {...},
  "architecture": {...}
}
```

**Пример ответа:**
```json
{
  "scope": {
    "project_title": "Согласование договоров",
    "project_goal": "Автоматизировать процесс согласования договоров",
    "entities_to_develop": [
      {
        "name": "Согласование договоров",
        "type": "Бизнес-процесс",
        "purpose": "Автоматизация процесса"
      }
    ],
    "functional_requirements": [
      "Автоматизировать процесс из 3 шагов",
      "Обеспечить работу ролей: Менеджер, Директор"
    ]
  },
  "markdown": "# Техническое задание: Согласование договоров\n\n..."
}
```

## Полный пайплайн

Для выполнения всех трех этапов последовательно используйте:

```bash
POST /api/agents/full-pipeline
```

**Пример запроса:**
```json
{
  "transcript": "У нас есть процесс согласования договоров..."
}
```

**Пример ответа:**
```json
{
  "as_is": {...},
  "architecture": {...},
  "scope": {...},
  "scope_markdown": "# Техническое задание...",
  "metadata": {
    "extraction_confidence": 0.85,
    "architecture_confidence": 0.75,
    "components_count": 2,
    "steps_count": 3
  }
}
```

## Интеграция с Telegram ботом

Агенты могут быть интегрированы в Telegram бота для удобной работы через чат.

## Примеры использования

См. [examples/agents/](examples/agents/) для примеров использования каждого агента.

