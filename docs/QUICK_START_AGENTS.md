# Быстрый старт: Агенты с LLM

## 🚀 Установка и настройка

### 1. Установите зависимости

```bash
pip install openai  # или anthropic
```

### 2. Настройте API ключ

Добавьте в `.env`:

```env
OPENAI_API_KEY=sk-ваш_ключ_здесь
# или
ANTHROPIC_API_KEY=sk-ant-ваш_ключ_здесь

LLM_PROVIDER=openai  # или anthropic
```

### 3. Запустите API сервер

```bash
uvicorn app.main:app --reload
```

## 📝 Использование

### Полный пайплайн (рекомендуется)

```bash
curl -X POST http://localhost:8000/api/agents/full-pipeline \
  -H "Content-Type: application/json" \
  -d '{
    "transcript": "У нас процесс согласования договоров. Менеджер создает заявку, отправляет директору на согласование. Директор проверяет и либо одобряет, либо отклоняет. Проблема в том, что это все делается вручную через почту."
  }'
```

**Ответ:**
```json
{
  "as_is": {
    "process_name": "Согласование договоров",
    "goal": "...",
    "actors": [...],
    "steps": [...],
    "pain_points": [...]
  },
  "architecture": "АРХИТЕКТУРНОЕ РЕШЕНИЕ\n\n1. Общий принцип...",
  "scope": "ТЕХНИЧЕСКОЕ ЗАДАНИЕ ДЛЯ СОГЛАСОВАНИЯ\n\n1. Цель проекта...",
  "metadata": {...}
}
```

### Только Process Extractor

```bash
curl -X POST http://localhost:8000/api/agents/process-extract \
  -H "Content-Type: application/json" \
  -d '{
    "transcript": "У нас процесс согласования договоров..."
  }'
```

## 🔍 Что происходит внутри

1. **Process Extractor (LLM)**
   - Анализирует транскрибацию
   - Извлекает структурированный AS-IS процесс
   - Возвращает JSON с акторами, шагами, сущностями

2. **Architect Agent (LLM + MCP)**
   - Получает AS-IS процесс
   - Использует MCP для поиска документации ELMA365
   - Проектирует архитектуру решения
   - Возвращает структурированный текст

3. **Scope Agent (LLM)**
   - Получает AS-IS + Архитектуру
   - Генерирует краткое ТЗ для согласования
   - Возвращает формальный документ (1-2 страницы)

## ⚙️ Настройка

### Выбор модели

По умолчанию используется:
- OpenAI: `gpt-4o-mini`
- Anthropic: `claude-3-haiku-20240307`

Изменить можно в коде или через переменные окружения.

### Параметры генерации

В `app/agents/pipeline.py` можно настроить:
- `temperature` (0.0-1.0) - креативность ответов
- `max_tokens` - максимальная длина ответа

## 🐛 Отладка

Если LLM не работает:
1. Проверьте API ключ в `.env`
2. Проверьте установку библиотеки (`pip install openai`)
3. Проверьте логи сервера

Система работает в режиме заглушки, если LLM не настроен.

## 📚 Дополнительная документация

- [AGENTS_LLM.md](AGENTS_LLM.md) - полная документация
- [AGENTS.md](AGENTS.md) - общая информация об агентах

