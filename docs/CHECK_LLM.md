# Проверка настройки LLM

## ✅ Что вы сделали

Вы добавили переменные в `.env` файл вручную. Это правильно!

## 🔍 Проверка конфигурации

### Вариант 1: Через скрипт

```bash
python scripts/check_llm_config.py
```

Скрипт покажет:
- Какой провайдер выбран
- Какие ключи установлены
- Готов ли провайдер к работе

### Вариант 2: Тест LLM клиента

```bash
python scripts/test_llm.py
```

Этот скрипт:
- Проверит инициализацию клиента
- Отправит тестовый запрос к LLM
- Покажет, работает ли все корректно

## 📝 Минимальные переменные для DeepSeek

Убедитесь, что в `.env` есть:

```env
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=ваш_ключ_здесь
```

Этого достаточно для работы с DeepSeek!

## 🚀 Проверка работы агентов

После настройки проверьте работу агентов:

```bash
# Запустите API
uvicorn app.main:app --reload

# В другом терминале проверьте endpoint
curl -X POST http://localhost:8000/api/agents/process-extract \
  -H "Content-Type: application/json" \
  -d '{"transcript": "У нас процесс согласования договоров. Менеджер создает заявку."}'
```

Если все настроено правильно, вы получите структурированный JSON ответ вместо заглушки.

## ❓ Если что-то не работает

1. **Проверьте .env файл:**
   - Убедитесь, что `LLM_PROVIDER=deepseek`
   - Убедитесь, что `DEEPSEEK_API_KEY` указан

2. **Проверьте библиотеку:**
   ```bash
   pip install openai
   ```
   (DeepSeek использует OpenAI-совместимый API)

3. **Проверьте ключ:**
   - Убедитесь, что ключ действителен
   - Проверьте баланс на https://platform.deepseek.com

4. **Проверьте логи:**
   При запуске API вы увидите предупреждения, если что-то не настроено.

