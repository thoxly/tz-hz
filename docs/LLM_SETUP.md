# Настройка LLM для агентов

Система поддерживает несколько провайдеров LLM. Выберите подходящий вариант.

## 🎯 Варианты настройки

### 1. Ollama (локальные модели) - РЕКОМЕНДУЕТСЯ

**Преимущества:**
- ✅ Бесплатно
- ✅ Работает локально (без интернета)
- ✅ Не требует API ключей
- ✅ Приватность данных

**Установка:**

1. Установите Ollama: https://ollama.ai

2. Скачайте модель:
```bash
ollama pull llama3
# или
ollama pull mistral
# или
ollama pull qwen2.5
```

3. Настройте `.env`:
```env
LLM_PROVIDER=ollama
LLM_MODEL=llama3  # или другая модель
OLLAMA_BASE_URL=http://localhost:11434
```

4. Запустите Ollama:
```bash
ollama serve
```

**Готово!** Агенты будут использовать локальную модель.

---

### 2. DeepSeek

**Преимущества:**
- ✅ Дешевле OpenAI
- ✅ Хорошее качество
- ✅ OpenAI-совместимый API

**Установка:**

1. Получите API ключ: https://platform.deepseek.com

2. Настройте `.env`:
```env
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=sk-ваш_ключ
DEEPSEEK_BASE_URL=https://api.deepseek.com
LLM_MODEL=deepseek-chat
```

3. Установите библиотеку:
```bash
pip install openai
```

---

### 3. OpenAI

**Установка:**

1. Получите API ключ: https://platform.openai.com

2. Настройте `.env`:
```env
LLM_PROVIDER=openai
OPENAI_API_KEY=sk-ваш_ключ
LLM_MODEL=gpt-4o-mini
```

3. Установите библиотеку:
```bash
pip install openai
```

---

### 4. Anthropic (Claude)

**Установка:**

1. Получите API ключ: https://console.anthropic.com

2. Настройте `.env`:
```env
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-ваш_ключ
LLM_MODEL=claude-3-haiku-20240307
```

3. Установите библиотеку:
```bash
pip install anthropic
```

---

## 🔧 Проверка настройки

После настройки проверьте работу:

```bash
# Запустите API
uvicorn app.main:app --reload

# Проверьте endpoint
curl -X POST http://localhost:8000/api/agents/process-extract \
  -H "Content-Type: application/json" \
  -d '{"transcript": "Тест"}'
```

Если все настроено правильно, вы получите структурированный ответ вместо заглушки.

---

## 📊 Сравнение провайдеров

| Провайдер | Стоимость | Скорость | Качество | Приватность |
|-----------|-----------|----------|----------|-------------|
| Ollama | Бесплатно | Средняя | Хорошее | ✅ 100% |
| DeepSeek | Низкая | Быстрая | Отличное | ⚠️ Облако |
| OpenAI | Средняя | Быстрая | Отличное | ⚠️ Облако |
| Anthropic | Высокая | Быстрая | Отличное | ⚠️ Облако |

---

## 🐛 Решение проблем

### Ollama не отвечает

1. Проверьте, что Ollama запущен:
```bash
ollama serve
```

2. Проверьте доступность:
```bash
curl http://localhost:11434/api/tags
```

3. Проверьте модель:
```bash
ollama list
```

### API ключ не работает

1. Проверьте правильность ключа в `.env`
2. Убедитесь, что ключ не истек
3. Проверьте баланс аккаунта (для платных сервисов)

### Модель не найдена (Ollama)

Скачайте модель:
```bash
ollama pull llama3
```

---

## 💡 Рекомендации

**Для разработки:** Используйте Ollama с моделью `llama3` или `mistral`

**Для продакшена:** 
- Если нужна приватность → Ollama
- Если нужна скорость и качество → DeepSeek или OpenAI

