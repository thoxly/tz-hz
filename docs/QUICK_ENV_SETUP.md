# Быстрая настройка переменных окружения

## ✅ Что уже сделано

Скрипт `scripts/setup_env.py` автоматически:
- ✅ Сохранил вашу существующую переменную `DEEPSEEK_API_KEY`
- ✅ Добавил все необходимые переменные для LLM провайдеров
- ✅ Не перезаписал существующие значения

## 📝 Что нужно проверить в .env

Откройте файл `.env` и убедитесь, что есть следующие переменные:

```env
# Провайдер LLM (выберите один)
LLM_PROVIDER=deepseek

# Ваш существующий ключ DeepSeek (должен быть сохранен)
DEEPSEEK_API_KEY=ваш_ключ_здесь

# Дополнительные переменные (уже добавлены скриптом)
DEEPSEEK_BASE_URL=https://api.deepseek.com
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
OLLAMA_BASE_URL=http://localhost:11434
LLM_MODEL=
```

## 🚀 Готово к использованию!

Если `DEEPSEEK_API_KEY` уже указан и `LLM_PROVIDER=deepseek`, система готова к работе.

### Проверка работы:

```bash
# Запустите API
uvicorn app.main:app --reload

# Проверьте endpoint
curl -X POST http://localhost:8000/api/agents/process-extract \
  -H "Content-Type: application/json" \
  -d '{"transcript": "Тест"}'
```

## 🔄 Если нужно изменить провайдер

Просто измените `LLM_PROVIDER` в `.env`:

- `LLM_PROVIDER=deepseek` - для DeepSeek (у вас уже есть ключ)
- `LLM_PROVIDER=openai` - для OpenAI (нужен OPENAI_API_KEY)
- `LLM_PROVIDER=anthropic` - для Anthropic (нужен ANTHROPIC_API_KEY)
- `LLM_PROVIDER=ollama` - для локальных моделей (не требует ключей)

