# Шаблон переменных окружения для .env

Скопируйте эти переменные в ваш `.env` файл. Если у вас уже есть `DEEPSEEK_API_KEY`, она будет сохранена.

## 📝 Переменные для LLM провайдеров

```env
# ============================================================================
# LLM Providers - выберите один провайдер
# ============================================================================

# Провайдер LLM (openai, anthropic, deepseek, ollama)
LLM_PROVIDER=deepseek

# Модель (если не указана, используется по умолчанию для провайдера)
LLM_MODEL=

# OpenAI (если используете OpenAI)
OPENAI_API_KEY=

# Anthropic (если используете Claude)
ANTHROPIC_API_KEY=

# DeepSeek (если используете DeepSeek) - У ВАС УЖЕ ЕСТЬ ЭТА ПЕРЕМЕННАЯ
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Ollama (если используете локальные модели)
# Не требует API ключа, но нужно установить Ollama: https://ollama.ai
OLLAMA_BASE_URL=http://localhost:11434
```

## 🔧 Как добавить в существующий .env

1. Откройте ваш `.env` файл
2. Добавьте переменные выше (если их еще нет)
3. Убедитесь, что `DEEPSEEK_API_KEY` уже указан (не перезаписывайте его!)
4. Установите `LLM_PROVIDER=deepseek` (или другой провайдер)

## ✅ Быстрая проверка

После добавления переменных проверьте:

```bash
# Запустите скрипт для проверки
python scripts/setup_env.py
```

Скрипт автоматически:
- Сохранит существующие переменные (включая DEEPSEEK_API_KEY)
- Добавит недостающие переменные
- Не перезапишет существующие значения

